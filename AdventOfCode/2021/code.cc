#include "../utils.cc"

void solve() {
    ll res = 0;
    vector<ll> inp;
    vector<string> ss = getInp([&](auto& cin, int i) {
        //ll _k; cin >> _k; inp.pb(_k);
        /*
        ll k; char c;
        do {
            cin >> k;
            inp.pb(k);
        } while(cin >> c);
        */
    });

    /*
    ss.pb("");
    for(string& s : ss) {
        if(s == "") {

        } else {

        }
    }
    */

    cout << res << endl;
}

